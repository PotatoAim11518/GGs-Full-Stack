doctype html
html
  head
    block head
      title=title
      link(rel='stylesheet' href='/stylesheets/reset.css')
  body
    block body
      append body
        nav.nav-container
          a(href='/')
            img.logo-img(src="/logo.png")
          div(class="navbar-sameLine")
            div(class="navbar-left")
              a.nav-item.nav-link(href='/allGames') Games
              a.nav-item.nav-link(href='/allGenres') Genres
              a.nav-item.nav-link(href='/allPublishers') Publishers
            div(class="navbar-right")
              a.nav-item.nav-link(href='/users/login') Sign in
              a.nav-item.nav-link(href='/users/signup') Sign up
              a.nav-item.nav-link(href='/') Log Out
  block content
